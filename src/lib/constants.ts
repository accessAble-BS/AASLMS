export { ROLE_SLUGS } from '@aas/shared-core';

export const LMS_EDITOR_ROLES = ['super_admin', 'lms_editor'] as const;

export const LMS_ACCESS_ROLES = ['super_admin', 'lms_editor', 'lms_learner'] as const;
